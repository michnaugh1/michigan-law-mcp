[Unit]
Description=Run the Michigan law nightly full crawl (safety net; see mlaw-feed-refresh.timer for the hourly feed-driven refresh)

[Timer]
OnCalendar=*-*-* 04:30:00 America/Detroit
Persistent=true
RandomizedDelaySec=600

[Install]
WantedBy=timers.target
